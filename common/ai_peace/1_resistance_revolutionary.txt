# Virtually the same thing as 0_resistance_yagooist_purist but with minor number differences
# Check there for the rationale

hloi_resistance_revolutionary_peace = {
	enable = {
		has_government = communism
	}

	annex_randomness = 0 
	liberate_randomness = 50 
	puppet_randomness = 50 
	take_states_randomness = 0 
	force_government_randomness = 100 
	
	# ROOT = Taker, FROM = Giver
	annex = {
		factor = 35
		
		modifier = {
			factor = 0
			OR = {
				NOT = {
					has_annex_war_goal = FROM
				}
				NOT = {
					is_owner_neighbor_of = FROM
				}
			}
			FROM = {
				any_owned_state = { 
					NOT = {
						OR = { 
							is_claimed_by = ROOT
							is_core_of = ROOT
							has_claimed_state_in_peace_conference = ROOT
						}
					}
				}
			}
		}
		#modifier = {
		#	factor = 0
		#	is_puppet = yes
		#}		
	}

	# ROOT = Taker, FROM = Liberated
	liberate = { #Liberate might be buggy and might require country-specific workarounds...
		factor = 700 

		modifier = {
			factor = 0

			NOT = {
				ai_liberate_desire = {
					target = FROM
					value > 0
				}
			}		
			NOT = {
				FROM = {
					has_government = democratic
					has_government = pro_yagoo_progressive
					has_government = mixed_ideology_democracy
				}			
			}
		}
				
		modifier = {
			factor = 300 #Strip colonies/liberate first - important vs GER and JAP
			FROM = {
				any_owned_state = { NOT = { is_core_of = FROM } }
			}
		}
	}

	# ROOT = Taker, FROM = Giver
	puppet = {
		factor = 100
		modifier = {
			factor = 0
			NOT = { is_owner_neighbor_of = FROM }
			NOT = {
				FROM = {
					any_neighbor_country = {
						is_puppet_of = ROOT
					}
				}
			}
			NOT = {
				FROM = {
					any_owned_state = {
						any_neighbor_state = {
							OR = {
								owner = { tag = ROOT }
								owner = { is_puppet_of = ROOT }
								has_claimed_state_in_peace_conference = ROOT
							}
						}
					}
				}
			}
			OR = {
				FROM = { capital_scope = { is_on_continent = europe } }
				NOT = {
					FROM = {
						any_owned_state = { 
							OR = {
								is_coastal = yes
								is_island_state = yes
							}
						}
					}
				}
				NOT = { any_owned_state = { OR = { is_coastal = yes is_island_state = yes } } }
			}
		}
		modifier = {
			factor = 100 #Yagoo is naughty >:((
			FROM = {
				tag = YAG
			}
		}		
		modifier = {
			factor = 100 #Strip colonies/liberate first - important vs GER and JAP
			FROM = {
				any_owned_state = { NOT = { is_core_of = FROM } }
			}
		}
		modifier = { 
			factor = 0
			OR = {
				AND = {
					#Is NOT a major
					NOT = { num_of_factories > 100 }
				}
				is_puppet = yes
			}
		}
		modifier = {
			factor = 0
			NOT = { is_owner_neighbor_of = FROM }
			#Is NOT a major
			NOT = { num_of_factories > 100 }
		}
		modifier = { #Don't puppet nations in civil war... this is too buggy.
			factor = 0
			FROM = { has_civil_war = yes }
		}
	}

	# ROOT = Taker, FROM = Giver
	puppet_all = {
		base = 20
	}
	
	# ROOT = STATE, FROM = Taker, FROM.FROM = Giver
	puppet_state = {
		base = 10

	}
	#Democracy ftw!
	force_government = {
		base = 900
		
		modifier = {
			factor = 100 #Yagoo is naughty >:((
			FROM = {
				tag = YAG
			}
		}	
	}
}
